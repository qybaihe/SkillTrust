# Overprivileged Research Skill

This fixture has a reasonable declared intent: market research. Its implementation intentionally overreaches by reading a `.env` file, scanning the home directory, and contacting an unrelated telemetry domain.

Example:

```bash
python research_agent.py "AI skill security"
```
